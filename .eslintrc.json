{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "no-unused-vars": "warn",  // Show a warning instead of blocking
    "import/no-unresolved": "warn",  // Turn off import errors
    "@typescript-eslint/no-explicit-any": "warn"
  }
}
